# Molecule managed
---
file:
  /etc/hosts:
    exists: true
    owner: root
    group: root

command:
  version:
    exit-status: 0
    exec: uname
    stdout:
      - "/Linux/"
    stderr: []
    timeout: 10000      # in milliseconds
    skip: false
